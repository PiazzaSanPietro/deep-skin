{
  "target": "pore",
  "run_name": "pore_run_10",
  "loss": "focal",
  "focal_gamma": 3.0,
  "oversample": true,
  "epochs": 80,
  "early_stopping_patience": 20,
  "scheduler_patience": 7,
  "batch_size": 8,
  "learning_rate": 1e-4,
  "weight_decay": 1e-3,
  "dropout_p": 0.3,
  "label_smoothing": 0.05
}
